export const ROWS = 8;
export const COLS = 8;
//define enum for color and corresponding color
const CellState = {
    EMPTY: null,
    BLACK: "black",
    WHITE: "white"
};

const Player1Color = CellState.BLACK;
const Player2Color = CellState.WHITE;