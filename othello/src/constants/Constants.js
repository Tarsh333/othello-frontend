export const ROWS = 8;
export const COLS = 8;
//define enum for color and corresponding color
export const CellState = {
    EMPTY: null,
    BLACK: "black",
    WHITE: "white"
};

export const Player1Color = CellState.BLACK;
export const Player2Color = CellState.WHITE;